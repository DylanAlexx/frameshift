[project]
name = "frameshift"
version = "0.1.0"
description = "An open-source media analysis platform for local video libraries."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "typer>=0.26.8",
]
